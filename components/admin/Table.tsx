import { ITableProps } from "@/types/uiElements";
import React from "react";

const Table: React.FC<ITableProps> = () => {
  return (
    // <Table
    //   {...tableProps}
    //   pagination={{ position: [top, bottom] }}
    //   columns={tableColumns}
    //   dataSource={[]}
    //   scroll={scroll}
    // />
    <></>
  );
};

export default Table;
